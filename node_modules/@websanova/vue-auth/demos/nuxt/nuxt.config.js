require('dotenv').config();
const { APP_KEY, BASE_URL } = process.env;


module.exports = {
    vendor: ['@websanova/vue-auth'],

    plugins: ['~plugins/vue-auth'],

    router: {
        middleware: 'auth'
    },
    env:{
        APP_KEY,
        BASE_URL
    }
}